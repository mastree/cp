
        //     int mid = (low + high + 1) / 2;
        //     if (ar[i] - ar[mid] <= bound){
        //         low = mid;
        //     } else{
        //         high = mid-1;
        //     }
        // }

        // if (i-low+1 < k) continue;
        // else if (query(1, 1, n, low-1, i-k)) update(1, 1, n, i, i, 1);